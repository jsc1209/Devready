server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅: 없는 경로는 index.html 로 폴백 (새로고침 404 방지)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
